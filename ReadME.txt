1. clone this repo
check into server and install dependencies and start the server
check into client folder and install the dependencies and start the react application
create an user table with (id,name,email,password,role) using mariaDB
